[tox]
skipsdist = true
envlist = tests, flake8

[testenv]
passenv = PYTEST_ADDOPTS DISPLAY
deps =
    bidpom
    PyPOM
    pytest~=3.0.0
    pytest-selenium
    pytest-xdist==1.15.0
commands = pytest {posargs}

[testenv:flake8]
skip_install = true
deps = flake8
commands = flake8 {posargs:.}
